How to run (REQUIRES PYTHON 3.11):

1. ./setup.ps1
2. python tts_cli_player_basic.py (can use the v2 one for clearer dialogue, save one for it to save directly)

GPU note:
- Setup installs CUDA-enabled PyTorch (`cu124`) inside `.venv`.
- Always activate `.venv` before running scripts.
